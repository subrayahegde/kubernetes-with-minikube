minikube start --driver=docker
eval $(minikube docker-env)  - This allows Minikube to see images you build locally, removing the need to push them to the cloud

docker logout - if at all needed.

In yaml file, ensure imagePullPolicy is set to Never (as we are using local docker env) 

docker build -t backend:latest ./backend
docker build -t frontend:latest ./frontend

kubectl apply -f backend.yaml
kubectl apply -f frontend.yaml

kubectl rollout restart deployment/backend
kubectl rollout restart deployment/frontend

Check your exact local image name by running: minikube image ls
minikube image rm frontend:v1 (If at all needed)
minikube image load frontend:v1 (If at all needed)

kubectl get nodes
kubectl get svc
kubectl get pods

minikube tunnel 

Note the external IP address exposed for backend service and use it in frontend for the URL of REST API. Rebuild the apps and rollout.


kubectl logs -f <pod-name>
kubectl describe pod <pod-name>








